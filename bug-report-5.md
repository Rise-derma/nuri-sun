# ぬりサン! v1.3.1 最終バグ監査・修正報告

作成日: 2026-08-05  
対象: GitHub Pages配布版 `index.html`

## 結論

v1.3.0を基準に、実機を使わず再現できる範囲をもう一度監査し、7件の問題を再現して修正しました。すべて恒久回帰テストへ追加し、Node回帰133項目と実Chrome/Chromium 197項目、合計330項目が合格しています。依存パッケージの`npm audit`は脆弱性0件、HTML構造検査も合格です。

現時点で、実機確認へ進むのを止める明らかな未修正バグは見つかっていません。ただし、テスト合格はバグが絶対にないことの保証ではありません。OSが表示する位置情報許可画面、実スピーカーからの音、iPhoneのホーム画面追加モード、ソフトウェアキーボードによる画面変形は実機で確認してください。

## v1.3.1で修正した問題

| 重要度 | 再現していた問題 | 修正内容 | 恒久テスト |
|---|---|---|---|
| 高 | localStorageの小さな動作確認は成功しても、本体データが容量不足で保存できない場合、警告が初回説明の裏へ隠れる | 保存失敗を継続状態として管理し、初回説明・設定・通常画面の現在見える場所へ表示。保存が回復したら自動で解除 | 実データだけ`QuotaExceededError`にするJSDOM試験と実Chrome試験、回復後の再保存 |
| 中 | `region-data.js`が読み込めても、内部の座標配列が壊れていると位置判定で例外が発生する | 全リング・座標を検証し、安全なポリゴンだけを判定に使用。内部破損時は手動地域設定の警告へ切替 | 8地域すべてへ壊れた入れ子座標を注入するJSDOM・実Chrome試験 |
| 中 | ページを開いたまま端末時計を記録時刻より前へ戻すと、設定3時間なのに「5:00:00」など長すぎる残り時間を表示する | 最終記録時刻が現在より未来になった場合はカウントダウンを無効扱いにし、次の操作時に保存値も正規化 | 10:30から8:00へ巻き戻す試験 |
| 中 | 時計を戻すと、未来時刻になった取得済みUV予測を「新しい」と誤判定して表示し続ける | UVキャッシュの経過時間が0以上1時間未満の場合だけ利用 | 2時間の時計巻き戻し試験 |
| 中 | 利用中の最新データ再読込が失敗した最初の操作では、失敗が画面に出ない経路がある | 再読込失敗と壊れた再読込値を即時に警告し、読めるまで上書きを防止 | 利用開始後の`store.get`故障注入 |
| 中 | 文字サイズを200%へ拡大するとタブバーの実高が増え、きせかえ最下段とトーストが下へ隠れる | タブバーを`ResizeObserver`で実測し、本文下余白とトースト位置を自動調整。再描画・画面変更時にも再計測 | 200%文字拡大、セーフエリア34px、280px幅、短い横画面 |
| 低 | 保存JSONのトップ階層に`__proto__`があると、`Object.assign`により状態オブジェクトのプロトタイプが変更される | 許可した状態キーだけを新しい既定オブジェクトへコピーし、旧版`avatar`だけ移行用に明示許可。無操作更新でも修復済み状態を保存 | `__proto__`混入、別タブ由来の汚染値、無操作トランザクション |

## 追加監査の範囲

### 保存・同時操作

* 初回・利用中の読込失敗、書込失敗、容量不足、削除失敗、メモリ保存へのフォールバック
* 壊れたJSON、JSONプリミティブ、100種類のランダム破損状態、未来日付・負数・不正な装備
* Web Locksありの3画面同時更新と、Web LocksなしのlocalStorageリースによる3タブ同時更新
* 記録・設定・地域変更の重なり、別タブの全削除、遅延報酬処理の取消
* `__proto__`と実行用HTML文字列の混入

### 時刻・通信・位置情報

* 日またぎ、月末、年末、うるう日、夏時間の23時間日と25時間日、時計の前進・巻き戻し
* 30分更新の抑制、拒否から許可への権限変化、権限取消、リセット後の遅延位置応答
* Open-MeteoのHTTP失敗、JSON形崩れ、文字列・`null`・`NaN`・無限大、古い応答と新しい応答の競合
* 位置座標の範囲、API送信前の小数2桁丸め、海外座標、島しょ部
* 2025年行政区域の1,902代表点と、地域境界ファイルの欠落・内部破損

### 画面・操作・アクセシビリティ

* 280×653、320×480、320×568、375×812、430×932、667×375、812×375、1280×800、2560×1440
* 文字200%拡大、上44px・下34pxのセーフエリア、縦横スクロール、固定タブとトーストの重なり
* 全タブ、初回説明、設定、報酬、購入2回確認、カテゴリ変更による確認取消
* キーボードフォーカス、モーダルのフォーカストラップ、`inert`、読み上げ用ライブ領域、動きを減らす設定
* axeによるWCAG 2.x違反検査と、主要な小文字配色の4.5:1コントラスト数値検査

### 配布物・静的検査

* HTMLの構造、ID・`for`・`aria-labelledby`参照、ボタン型、外部リンクのHTTPSと`noopener`
* 外部実行スクリプトがなく、同梱`region-data.js`だけを読むこと
* 58パーツ、21ヒント、承認済みの日焼け止め量表記、子ども向け漢字監査
* `npm ci`、`npm audit`、GitHub Actions、ZIP内の不要ファイル・パス・ハッシュ

## 最終テスト内訳

| コマンド | 件数 | 結果 |
|---|---:|---|
| `npm test` | 133 | 合格 |
| `npm run test:browser` | 197 | 合格 |
| 合計 | 330 | 全合格 |

## 実機で確認する項目

1. iPhone SafariとAndroid Chromeで「はじめる!」を押し、位置情報を許可すると「いまのよそう」へ変わる。
2. OS設定から位置情報を取り消すと「ちいきべつ」へ戻る。
3. 音をオンにし、画面を一度タップした後、実際のスピーカーから音が出る。
4. 端末を横向きにし、設定画面を先頭から最後まで指でスクロールできる。
5. 文字サイズを大きくし、きせかえ最下段とトーストがタブバーに隠れない。
6. iPhoneの「ホーム画面に追加」状態で、上端・下端・ソフトウェアキーボード表示時に操作が欠けない。
